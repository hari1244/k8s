apiVersion: v1
kind: Service
metadata:
  name: nginx-lb-service
spec:
  type: loadBalancer
  selector:
    project: roboshop
    component: frontend
    enironment: dev
    purpose: loadbalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30007
