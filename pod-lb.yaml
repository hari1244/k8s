apiVersion: v1
kind: Pod
metadata:
  name: nginx-lb-pod
  labels:
    project: roboshop
    component: frontend
    enironment: dev
    purpose: loadbalancer
spec:
  containers:
  - name: nginx
    image: nginx:1.14.2
    