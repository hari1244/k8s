apiVersion: v1
kind: Pod
metadata:
  name: pod-nodeport
spec:
  containers:
  - name: nginx
    image: nginx
    envFrom:
    - configMapRef:
        name: app-config
    ports:
    - containerPort: 80
      name: http  # Optional but recommended
      protocol: TCP